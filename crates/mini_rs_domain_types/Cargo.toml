[package]
name = "mini_rs_domain_types"
version = "1.0.0"
edition = "2024"
license = "Apache-2.0"
description = "Shared deterministic domain types for Mini RS ERP."
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
